
<div class="row mb-4 project">
    <div class="col-md-5">
        <h2>
        <a ></a>
        </h2>

{{!-- POST --}}
        <p>
            <span class="post-text">{{post.description}}</span>
        </p>
        <p>Created by {{post.user.name}} on {{format_date post.date_created}}</p>
        </div>
        <div class="col-md-7">
            <p>
                {{post.description}}
            </p>
    </div>
</div>   